[phases.setup]
nixPkgs = ["python311", "gcc", "glibc"]
aptPkgs = ["libgl1", "libglib2.0-0", "libsm6", "libxext6", "libxrender-dev", "libgomp1"]

[phases.install]
cmds = ["pip install -r requirements-cpu.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
